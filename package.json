{
  "name": "llm-usage-tracker",
  "displayName": "LLM Usage Tracker",
  "description": "Track usage limits across multiple LLM services (Claude Code, Codex, Antigravity, Gemini)",
  "version": "0.0.1",
  "publisher": "llm-usage-tracker",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "llmUsageTracker.refresh",
        "title": "Refresh Usage Data",
        "category": "LLM Usage"
      },
      {
        "command": "llmUsageTracker.openSettings",
        "title": "Open Dashboard",
        "category": "LLM Usage"
      },
      {
        "command": "llmUsageTracker.openDashboard",
        "title": "Open Dashboard",
        "category": "LLM Usage"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "llmUsageTracker",
          "title": "LLM Usage",
          "icon": "$(graph)"
        }
      ]
    },
    "views": {
      "llmUsageTracker": [
        {
          "id": "llmUsageTrackerSidebar",
          "name": "Usage Overview"
        }
      ]
    },
    "configuration": {
      "title": "LLM Usage Tracker",
      "properties": {
        "llmUsageTracker.pollingInterval": {
          "type": "number",
          "default": 60,
          "minimum": 10,
          "maximum": 300,
          "description": "Polling interval in seconds (10s - 5min)"
        },
        "llmUsageTracker.displayMode": {
          "type": "string",
          "enum": [
            "used",
            "remaining"
          ],
          "enumDescriptions": [
            "Show how much quota has been used.",
            "Show how much quota is left."
          ],
          "default": "used",
          "description": "Whether quota values are displayed as used or remaining."
        },
        "llmUsageTracker.statusBarTooltipLayout": {
          "type": "string",
          "enum": [
            "regular",
            "monospaced"
          ],
          "enumDescriptions": [
            "Show the status bar hover in the regular table style.",
            "Show the status bar hover in a fixed-width monospaced layout."
          ],
          "default": "regular",
          "description": "How the status bar hover tooltip is laid out."
        },
        "llmUsageTracker.hiddenServices": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "List of service names hidden from sidebar and status bar"
        },
        "llmUsageTracker.services": {
          "type": "object",
          "default": {
            "claudeCode": {
              "enabled": true
            },
            "codex": {
              "enabled": false
            },
            "antigravity": {
              "enabled": true
            },
            "gemini": {
              "enabled": false
            }
          },
          "description": "Service configuration"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "playwright:install": "playwright install chromium",
    "build:test-e2e": "tsc -p tests/e2e/tsconfig.json",
    "test": "npm run test:unit",
    "test:unit": "vitest run --config vitest.config.ts --coverage",
    "test:browser": "playwright test --config playwright.config.ts",
    "test:e2e": "npm run build:test-e2e && node out-test/e2e/run-tests.js",
    "test:all": "npm run test:unit && npm run test:browser && npm run test:e2e",
    "test:ci": "npm run compile && npm run test:unit && npm run test:browser && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^25.4.0",
    "@types/vscode": "^1.109.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
